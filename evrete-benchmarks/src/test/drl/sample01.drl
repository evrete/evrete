import org.evrete.benchmarks.models.misc.TypeA;
import org.evrete.benchmarks.models.misc.TypeB;
rule "sample01"
  when
      $a : TypeA()
      $b : TypeB( $a.i == $b.i )
  then
      $a.waitNs($a.getL());
end


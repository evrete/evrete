import org.evrete.benchmarks.models.ml.Image;
import org.evrete.benchmarks.models.ml.DataModel;

rule "imagess"
  when
      $img1        : Image()
      $img2        : Image(label.equals($img1.label))
      $model       : DataModel()
  then
      $model.compute($img1, $img2);
end


package org.evrete.api;

public interface StatelessSession extends RuleSession<StatelessSession> {

/*
    StatelessSession setFireCriteria(BooleanSupplier fireCriteria);

    RuntimeRule getRule(String name);

    default void insertAndFire(Collection<?> objects) {
        insert(objects);
        fire();
    }

    default void insertAndFire(Object... objects) {
        insert(objects);
        fire();
    }
*/

}

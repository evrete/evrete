package org.evrete.runtime;

/**
 * Abstract LHS (Left-hand side) representation of a rule. When rules are built and deployed, the LHS instances are transformed from one implementation to another.
 *
 * @param <FT> Fact type parameter
 */
public class LHS<FT extends AbstractLhsFact> {
}
